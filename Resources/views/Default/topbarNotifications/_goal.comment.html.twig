{% extends 'ABMundiNotificationBundle:Default:topbarNotifications/_default.html.twig' %}

{% block url %}{{ path('goal_show', { 'username': notification.lastEvent.subject.owner.username, 'slug': notification.lastEvent.subject.slug }) ~ "#comments" }}{% endblock %}

{% block message %}

    {% set event = notification.lastEvent %}

    <b>{{ event.actor.username }}</b>

    {% trans %}commented your goal{% endtrans %}  

    <b>{{event.subject.name}}</b>

{% endblock %}